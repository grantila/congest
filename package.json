{
  "name": "congest",
  "description": "Promise-wise await event handler before emitting next event",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "test": "node_modules/.bin/mocha"
  },
  "devDependencies": {
    "bluebird": "^3.0",
    "chai": "^3.4",
    "mocha": "~2.3"
  }
}
